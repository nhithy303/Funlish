<h2 class="text-center mt-2">Thông tin bài học</h2>
<form method="POST" action="/courses/lessons/{{lesson._id}}?_method=PUT" name="update-lessons-form" onsubmit="return validateForm(event)">
    <div class="mb-4">
        <label for="order" class="form-label">Thứ tự</label>
        <input type="text" class="form-control" id="order" name="order" value="{{lesson.order}}">
    </div>
    <div class="mb-4">
        <label for="name" class="form-label">Tên bài học</label>
        <input type="text" class="form-control" id="name" name="name" value="{{lesson.name}}">
    </div>
    <div class="mb-4">
        <label for="workbook" class="form-label">Tài liệu</label>
        <input type="text" class="form-control" id="workbook" name="workbook" value="{{lesson.workbook}}">
    </div>
    <div class="mb-4">
        <label for="video" class="form-label">Video</label>
        <input type="text" class="form-control" id="video" name="video" value="{{lesson.video}}">
    </div>
    <div class="mb-4">
        <label for="image" class="form-label">Hình ảnh</label>
        <input type="text" class="form-control" id="image" name="image" value="{{lesson.image}}">
    </div>
    <div class="mb-4">
        <label for="description" class="form-label">Mô tả</label>
        <textarea class="form-control" id="description" name="description">{{lesson.description}}</textarea>
    </div>
    <div class="mb-4 text-center">
        <button type="submit" class="btn btn-primary">Cập nhật</button>
    </div>
</form>

<script>
    const validateForm = (e) => {
        const updateLessonsForm = document.forms["update-lessons-form"];
        const order = updateLessonsForm["order"].value;
        const name = updateLessonsForm["name"].value;
        const workbook = updateLessonsForm["workbook"].value;
        const video = updateLessonsForm["video"].value;
        const image = updateLessonsForm["image"].value;
        const description = updateCoursesForm["description"].value;
        if (order === "" || name === "" || workbook === "" || video === "" || image === "" || description === "") {
            alert("Thông tin bài học còn trống!");
            e.preventDefault();
        }
    }
</script>