<nav class="navbar">
    <div class="container-fluid">
        <ul class="navbar-nav ms-auto me-auto w-100">
            {{#each courses}}
                <li class="nav-item color-courses mt-1 menu" id="{{this.slug}}">
                    <a class="nav-link" href="/courses/{{this.slug}}">{{this.name}}</a>
                </li>
            {{/each}}
        </ul>
    </div>
</nav>